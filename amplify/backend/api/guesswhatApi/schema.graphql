type User @model {
  id: ID!
  username: String!
  email: String
}

type Highscore @model @key(name: "ByPoints", fields: ["type", "points"], queryField: "getHighscoreByPoints") {
  id: ID!
  type: String!
  points: Int!
  ownerId: ID!
  numRounds: Int
  date: String
}
